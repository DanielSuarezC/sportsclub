import { ReunionAdministrativo } from './reunion-administrativo';

describe('ReunionAdministrativo', () => {
  it('should create an instance', () => {
    expect(new ReunionAdministrativo()).toBeTruthy();
  });
});
